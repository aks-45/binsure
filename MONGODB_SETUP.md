# MongoDB Atlas Setup

## Quick Setup
1. The project is already configured with MongoDB Atlas
2. Start the backend: `cd server && npm run dev`
3. Start the frontend: `npm run dev`
4. Submit the contact form - data saves to Atlas

## Atlas Details
- **Database**: binsure
- **Collection**: contacts
- **Connection**: Pre-configured in server/.env

Form submissions are automatically stored in MongoDB Atlas.